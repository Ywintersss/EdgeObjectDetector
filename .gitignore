# --- Large data trees (never commit; multi-GB) ---
dataset/
dataset_640/
dataset_640_trial/
coco_converted/
coco_converted-2/
yolo_format/
instances_*.json

# --- Generated pipeline artifacts ---
cutouts/
cutouts_trial/
backgrounds/
backgrounds_trial/
dataset_synth/
dataset_synth_trial/
dataset_synth*.yml
dataset_synth_coarse/

# --- Training outputs & model weights ---
runs/
*.pt
*.tflite
*.onnx

# --- Python ---
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/

# --- SDD scratch ---
.superpowers/

# derived real-scene domain-adaptation artifacts (rebuildable, non-destructive)
dataset_real/
studio_coarse/
dataset_real_blend.yml
eval_single_item.yml

# WSL twins of the dataset yamls: same absolute-machine-path problem as the originals
# above, just with /mnt/d instead of D:/. Regenerate by rewriting the drive prefix.
*.wsl.yml

# Ultralytics downloads this into the repo root during INT8 export.
calibration_image_sample_data_*.npy

# --- INT8 export artifacts (rebuildable; report.md is kept as a record) ---
export/*.tflite
deploy/*.tflite
deploy/classes.txt
